# fix-python skill

This repository uses Python 3.9+ and `pytest` for test coverage.

Use this skill to:
- make small, readable fixes to Python code
- follow PEP 8 style and clear function naming
- avoid adding new dependencies unless necessary
- update or add tests in `tests/test_user_table.py` for any code changes
- preserve existing public behavior unless the user explicitly requests changes
