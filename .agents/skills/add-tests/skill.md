# add-tests skill

This repository is small and Python-based. When adding or updating functionality, also add or update focused `pytest` tests.

Use this skill to:
- identify where existing tests live (`tests/test_user_table.py`)
- preserve the current `pytest` project structure
- avoid adding unnecessary dependencies
- keep tests small and directly tied to the change
