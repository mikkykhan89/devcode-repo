# explain-repo skill

This repository contains a simple Python project with a single script at `python_process/user_table.py` and a test at `tests/test_user_table.py`.

Use this skill to answer questions about the repository structure, how to run the project, and where key files are located.

Focus on:
- Python 3.9+ compatibility
- `pytest` test discovery using the `tests` folder
- the existing `README.md` and `.github/copilot-instructions.md` guidance
- the minimal entrypoint behavior in `python_process/user_table.py`
